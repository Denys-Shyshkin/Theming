import styled from 'styled-components';

const TitleContainer = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 60px;
  margin-bottom: 60px;
`;

const InfoContainer = styled.div`
  display: flex;
  align-items: center;
`;

export { TitleContainer, InfoContainer };
