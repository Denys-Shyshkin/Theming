import styled from 'styled-components';
import { StyledIconBase } from '@styled-icons/styled-icon';

const IconStyleWrapper = styled.div`
  ${StyledIconBase} {
    position: absolute;
    top: 3px;
    left: 6px;
  }
`;

export { IconStyleWrapper };
