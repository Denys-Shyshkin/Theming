import styled from 'styled-components';

const ReadTimeContainer = styled.div`
  display: flex;
  align-items: center;
  margin-bottom: 10px;
`;

const ReadTimeText = styled.p`
  margin-left: 5px;
`;

export { ReadTimeContainer, ReadTimeText };
