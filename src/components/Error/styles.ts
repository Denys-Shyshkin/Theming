import styled from 'styled-components';

const ErrorContainer = styled.div`
  width: fit-content;
  height: 40vh;
  margin: 100px auto;
`;

const ErrorText = styled.h3`
  font-style: italic;
`;

export { ErrorContainer, ErrorText };
